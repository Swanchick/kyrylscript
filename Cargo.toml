[workspace]
members = [
    "core",
    "cli",
    "ks_std",
    "vm",
    "global",
    "native",
]

resolver = "3"
default-members = ["cli"]

[workspace.dependencies]
core = { path = "core" }
vm = { path = "vm" }

[workspace.package]
version = "0.5.0"
authors = ["Kyryl Lebedenko"]
